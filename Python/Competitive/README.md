This folder contains:

1.Write a program which contains one function named as Display() that prints "Jay

Ganesh" on console.

2.Write a program which contains one function ChkGreater() that accepts two numbers

and prints the greater number.

3.Write a program which accepts one number and prints square of that number.

4.Write a program which accepts one number and prints cube of that number.

5.Write a program which accepts one number and checks whether it is divisible by 3 and

6.Write a program which accepts one number and prints multiplication table of that

number.

7.Write a program which accepts one number and prints sum of first N natural numbers.

8.Write a program which accepts one number and prints factorial of that number.

9.Write a program which accepts one number and prints all even numbers till that

number.

10.Write a program which accepts one number and prints all odd numbers till that number.

11.program which accepts one number and checks whether it is prime or not.

12.program which accepts one number and prints count of digits in that number.

13.program which accepts one number and prints sum of digits.

14.program which accepts one number and prints reverse of that number.

15.program which accepts one number and checks whether it is palindrome or not.

16.program which accepts one character and checks whether it is vowel or
consonant.

17.program which accepts one number and prints its factors.

18.program which accepts two numbers and prints addition, subtraction,

multiplication and division.

19.a program which accepts one number and prints that many numbers starting

from 1.

20.program which accepts one number and prints that many numbers in reverse

order.

21.a program which accepts length and width of rectangle and prints area.

22.program which accepts radius of circle and prints area of circle.

23.program which accepts one number and checks whether it is perfect number or

not.

24.program which accepts one number and prints binary equivalent.

25.a program which accepts marks and displays grade.

Condition Example:

• ≥ 75 → Distinction

• ≥ 60 → First Class

• ≥ 50 → Second Class

• < 50 → Fail

26. lambda function which accepts one number and returns square of that number.

27. lambda function which accepts one number and returns cube of that number.

28.. Write a lambda function which accepts two numbers and returns maximum number.

29. Write a lambda function which accepts two numbers and returns minimum number.

30. Write a lambda function which accepts one number and returns True if number is even

otherwise False.

31. Write a lambda function which accepts one number and returns True if number is odd

otherwise False.

32. Write a lambda function which accepts one number and returns True if divisible by 5.

33. Write a lambda function which accepts two numbers and returns addition.

34.lambda function which accepts two numbers and returns multiplication.

35. lambda function which accepts three numbers and returns largest number.

36. Write a lambda function using map() which accepts a list of numbers and returns a list of squares of

each number.

37. Write a lambda function using filter() which accepts a list of numbers and returns a list of even

numbers.

38. Write a lambda function using filter() which accepts a list of numbers and returns a list of odd

numbers.

39. Write a lambda function using reduce() which accepts a list of numbers and returns the addition of

all elements.

40. Write a lambda function using reduce() which accepts a list of numbers and returns the maximum

element.

41. Write a lambda function using reduce() which accepts a list of numbers and returns the minimum

element.

42. Write a lambda function using filter() which accepts a list of strings and returns a list of strings

having length greater than 5.

43. Write a lambda function using filter() which accepts a list of numbers and returns a list of numbers

divisible by both 3 and 5.

44. Write a lambda function using reduce() which accepts a list of numbers and returns the product of all

elements.

45.Write a lambda function using filter() which accepts a list of numbers and returns the count of even

numbers.



































































